// Bucle en el que se cuenta desde el numero ingresado hasta el 100

for (let i = parseInt(prompt("Ingrese un numero del 1 al 100")); (i >= 1) && (i <= 100) && (isNaN = i); i++) {
    console.log(i);
}
